# Soccerbot — ROS 2 Humble workspace image (stub)
# Base image: official ROS 2 Humble
FROM ros:humble

# TODO: build steps go here (e.g. rosdep install, colcon build).
# No real build steps yet — this is a stub.
